@model IEnumerable<ocbc_team1.Models.BankAccount>
@{
    ViewData["Title"] = "Dashboard";
}

<h2 class="pb-5">Your Accounts</h2>


@{
    int counter = 0;

    //  Template loop, use when DAL ready.

    @*@foreach (var item in Model)
    {
        <div class="container mx-auto shadow bg-white rounded p-5">
            <h4 class="pb-4">Savings Acount - @item.CardNumber</h4>
            <div class="row justify-content-end">
                <div class="col-3">Account Balance @item.AmountAvaliable</div>
                <div class="col-3">Total Balance @item.AmountRemaining</div>
            </div>
        </div>
        counter += 1;
    }
    @if (counter == 0)
    {
        <span style="color:red">No active accounts</span>
    }*@

    <div class="container mx-auto shadow bg-white rounded p-5">
        <h4 class="pb-4">Savings Acount - 000-00000-0</h4>
        <div class="row justify-content-end">
            <div class="col-4">Account Balance $00.00</div>
            <div class="col-4">Total Balance $00.00</div>
        </div>
    </div>
}
